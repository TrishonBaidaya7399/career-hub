// import PropTypes from 'prop-types';

const Statistics = () => {
    return (
        <div>
            <h1>Statistics page</h1>
        </div>
    );
};

Statistics.propTypes = {
    
};

export default Statistics;